# Temporary: @headlessui/react@1.7.17 does not declare React 19 as a
# supported peer, so npm install hard-fails without this. Delete this
# file once @headlessui/react is removed from the project.
legacy-peer-deps=true
